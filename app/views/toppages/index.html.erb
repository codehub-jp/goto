<% if test_logged_in? %>
    <p>ログイン完了ユーザ: <%= test_current_user.name %></p>
<% else %>
    <div class="center jumbotron">
        <div class="text-center">
            <h1>Hello GoTo!</h1>
            <%= link_to '新しく登録する', signup_path, class: 'btn btn-lg btn-primary' %>
        </div>
    </div>
<% end %>
